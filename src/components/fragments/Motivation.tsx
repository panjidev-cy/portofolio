import PaddingScreen from "../shared/PaddingScreen";
const Motivation = () => {
  return (
    <div className="font-jakarta py-40 w-full h-auto">
      <PaddingScreen>
        <div className="text-center">
          <h1>
            I don’t just build websites — I build <i>experiences</i> that tell stories.
          </h1>
        </div>
      </PaddingScreen>
    </div>
  );
};

export default Motivation;
